# buggy-cart

A tiny shopping-cart library used as a benchmark task for the agent.

```bash
npm test
```

The test suite documents the intended behaviour. The implementation in `src/cart.js`
does not currently match it.
